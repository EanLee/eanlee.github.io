---
const today = new Date();
---

<footer>
  <div class="footer-container">
    <div class="footer-content">
      
      <!-- 個人資訊區域 -->
      <div class="footer-about">
        <div class="author-info">
          <div class="author-name">伊恩</div>
          <div class="author-role">.NET Developer | Blogger | Learner</div>
        </div>
        
        <div class="author-descriptions">
          <p class="author-description">
            在知識大海中漂流的小船，不停追尋屬於自己的秘寶。關注各種架構與技術的發展，並持續探索技術與業務價值的平衡點。
          </p>
          <p class="author-focus">
            目前專注於.NET、Cloud Native、系統架構、容器化與監控的統整，在支撐業務價值的同時，有效降低建置/營運成本、並提升系統的可靠性。
          </p>
        </div>
      </div>

      <!-- 連結區域 -->
      <div class="footer-links">
        <div class="link-section">
          <div class="section-title">Projects</div>
          <a href="https://url-ins.com" title="Url-Insight" target="_blank" rel="noopener">
            Url-Insight
          </a>
        </div>

        <div class="link-section">
          <div class="section-title">Social</div>
          <a href="https://facebook.com/276561129952456" title="Facebook" target="_blank" rel="noopener noreferrer" class="social-link">
            <span class="social-icon">📘</span>
            Facebook
          </a>
          <a href="https://www.linkedin.com/in/ean-lee-7b63b6110/" title="LinkedIn" target="_blank" rel="noopener noreferrer" class="social-link">
            <span class="social-icon">💼</span>
            LinkedIn
          </a>
        </div>
      </div>
      
    </div>
  </div>

  <!-- 版權區域 -->
  <div class="footer-bottom">
    <div class="bottom-content">
      <p class="disclaimer">
        文章內容均為個人學習記錄、心得與認知，若任何謬誤或建議，歡迎直接留言討論，一同成長。
      </p>
      <p class="copyright">
        &copy; 2021-{today.getFullYear()} 伊恩的軟體狂想 All rights reserved.
      </p>
    </div>
  </div>
</footer>

<style>
  footer {
    margin-top: var(--spacing-3xl);
    background: var(--color-bg-secondary);
    border-top: 1px solid var(--color-border-light);
  }

  .footer-container {
    max-width: var(--container-xl);
    margin: 0 auto;
    padding: 0 var(--spacing-md);
  }

  .footer-content {
    padding: var(--spacing-xl) 0 var(--spacing-lg) 0;
    display: grid;
    grid-template-columns: 2fr 1fr;
    gap: var(--spacing-xl);
    border-bottom: 1px solid var(--color-border-light);
  }

  /* 個人資訊區域 */
  .footer-about {
    max-width: 600px;
  }

  .author-info {
    margin-bottom: var(--spacing-md);
  }

  .author-name {
    font-size: var(--font-size-xl);
    font-weight: var(--font-weight-bold);
    color: var(--text-primary);
    margin: 0 0 var(--spacing-xs) 0;
  }

  .author-role {
    font-size: var(--font-size-sm);
    color: var(--text-secondary);
    margin: 0;
    font-weight: var(--font-weight-medium);
  }

  .author-descriptions {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-sm);
  }

  .author-description {
    font-size: var(--font-size-sm);
    color: var(--text-secondary);
    line-height: var(--line-height-relaxed);
    margin: 0;
    font-style: italic;
  }

  .author-focus {
    font-size: var(--font-size-sm);
    color: var(--text-secondary);
    line-height: var(--line-height-relaxed);
    margin: 0;
    font-weight: var(--font-weight-medium);
  }

  /* 連結區域 */
  .footer-links {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-lg);
  }

  .link-section {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-sm);
  }

  .section-title {
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-semibold);
    color: var(--text-primary);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .link-section a {
    font-size: var(--font-size-sm);
    color: var(--text-secondary);
    text-decoration: none;
    transition: color var(--transition-normal);
    padding: var(--spacing-xs) 0;
  }

  .link-section a:hover {
    color: var(--color-primary);
  }

  .social-link {
    display: flex;
    align-items: center;
    gap: var(--spacing-xs);
  }

  .social-icon {
    font-size: var(--font-size-base);
    line-height: 1;
  }

  /* 版權區域 */
  .footer-bottom {
    background: rgba(var(--color-black), 0.02);
  }

  .bottom-content {
    max-width: var(--container-xl);
    margin: 0 auto;
    padding: var(--spacing-sm);
    text-align: center;
    display: flex;
    flex-direction: column;
    gap: var(--spacing-xs);
  }

  .disclaimer {
    font-size: var(--font-size-xs);
    color: var(--text-secondary);
    line-height: var(--line-height-relaxed);
    margin: 0;
  }

  .copyright {
    font-size: var(--font-size-xs);
    color: var(--text-muted);
    margin: 0;
    font-weight: var(--font-weight-medium);
  }

  /* 響應式設計 */
  @media (max-width: 768px) {
    .footer-content {
      grid-template-columns: 1fr;
      gap: var(--spacing-xl);
      text-align: center;
    }

    .footer-about {
      max-width: none;
    }

    .footer-links {
      flex-direction: row;
      justify-content: center;
      gap: var(--spacing-2xl);
    }
  }

  @media (max-width: 480px) {
    .footer-content {
      padding: var(--spacing-xl) 0;
      gap: var(--spacing-lg);
    }

    .footer-links {
      flex-direction: column;
      gap: var(--spacing-lg);
    }

    .author-name {
      font-size: var(--font-size-lg);
    }

  }

  @media (max-width: 414px) {
    .footer-container {
      padding: 0 var(--spacing-sm);
    }

    .bottom-content {
      padding: var(--spacing-sm);
    }
  }
</style>

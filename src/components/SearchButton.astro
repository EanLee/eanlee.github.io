---
// SearchButton.astro - A search button that navigates to search page
---

<a href="/search/" class="search-button" title="搜尋文章">
  <svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="search-icon">
    <circle cx="11" cy="11" r="8"></circle>
    <line x1="21" y1="21" x2="16.65" y2="16.65"></line>
  </svg>
</a>

<style>
  .search-button {
    display: flex;
    align-items: center;
    justify-content: center;
    padding: var(--spacing-sm);
    color: var(--text-secondary);
    text-decoration: none;
    cursor: pointer;
    transition: all var(--transition-fast);
    border-radius: var(--radius-sm);
  }

  .search-button:hover {
    color: var(--color-primary);
    background: rgba(35, 55, 255, 0.1);
    transform: scale(1.1);
  }

  .search-icon {
    transition: all var(--transition-fast);
  }
</style>